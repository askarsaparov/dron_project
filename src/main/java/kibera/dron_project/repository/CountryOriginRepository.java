package kibera.dron_project.repository;

import kibera.dron_project.domain.CountryOrigin;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CountryOriginRepository extends JpaRepository<CountryOrigin, Long> {
}
